# 健身房管理系统 — 项目答辩稿

> 以下以答辩学生口吻撰写，可直接用于项目介绍。

---

## 一、项目背景与定位

各位老师好，我今天要介绍的项目是 **"FitLife 智能健身预约系统"**。

这个项目的定位是面向中小型健身房的一站式数字化管理平台。传统健身房在课程预约、会员管理上大量依赖人工，效率低、容易出错，而且高峰期热门课程抢购时容易出现超卖。我的项目目标就是通过技术手段解决这些痛点，提供一套从课程管理、在线预约、支付结算到会员体系的完整解决方案。

项目采用 **B/S 架构**，前后端分离，用户无需安装任何客户端，打开浏览器就能使用。

---

## 二、技术栈

项目采用当前主流的全栈技术方案：

| 层面 | 技术选型 | 版本 |
|---|---|---|
| 后端框架 | Spring Boot 3 | 3.2.2 |
| 开发语言 | Java 17 | — |
| ORM 框架 | MyBatis-Plus | 3.5.5 |
| 数据库 | MySQL | 8.0 |
| 缓存/分布式锁 | Redis + Redisson | 3.27.0 |
| JWT 鉴权 | Hutool JWT | 5.8.25 |
| 密码加密 | BCrypt（Spring Security Crypto） | — |
| 在线支付 | 支付宝沙箱 SDK | 4.38.61 |
| AI 对话 | DeepSeek API（deepseek-chat） | — |
| 前端框架 | Vue 3（Composition API） | 3.4.19 |
| 前端构建 | Vite | 5.1.4 |
| UI 组件库 | Element Plus | 2.5.6 |
| 数据可视化 | ECharts 6 | 6.0.0 |
| 路由管理 | Vue Router 4 | 4.2.5 |

**为什么选 Spring Boot + Vue？**

Spring Boot 是 Java 生态中最成熟的微服务框架，自动配置、起步依赖大幅降低开发成本，生态丰富，社区活跃。Vue 3 采用 Composition API，逻辑复用能力强，配合 Vite 构建工具，开发体验极佳，热更新几乎是秒级。两者都是目前业界和就业市场的主流选择，技术风险低，学习资料丰富。

---

## 三、核心功能模块

系统包含两大角色视角：

### 3.1 普通用户端

1. **课程浏览与预约**：按分类（有氧训练、力量训练、瑜伽、动感单车、搏击、舞蹈共 6 类）筛选课程，查看课程详情（教练、时间、价格、剩余名额），一键预约。
2. **我的预约**：查看预约记录，支持待支付订单的余额支付和取消操作。已支付订单取消后自动退款并回退库存。
3. **钱包与 VIP**：余额充值、购买月卡（30 元/月，享 9 折）或年卡（300 元/年，享 8 折），续费在原有效期上叠加，不损失剩余时长。
4. **个人中心**：修改昵称、手机、邮箱、性别、密码等个人资料。
5. **AI 智能客服**：自建 AI 对话页面，支持 SSE 流式输出，能根据用户实时余额、VIP 状态、可预约课程给出个性化推荐，不代替用户操作，只做顾问。
6. **数据驾驶舱**：首页 ECharts 图表展示预约趋势、课程热度排行、分类统计等数据。

### 3.2 管理员端

1. **课程管理**：课程的增删改查，支持分类筛选和搜索。
2. **用户管理**：用户列表、账号启用/禁用（不能禁用自己）。
3. **Banner 管理**：首页轮播图的上下架管理。
4. **数据驾驶舱**：总用户数、总订单数、总收入、VIP 分布等运营数据统计。

---

## 四、架构设计

### 4.1 后端分层架构

```
Controller → Service(接口+实现分离) → Mapper → MySQL
     ↓
  strategy/   (DiscountFactory + DiscountStrategy)
     ↓
  common/     (Result<T>、GlobalExceptionHandler、BusinessException、ErrorCode)
     ↓
  config/     (JwtInterceptor、CorsConfig、AliPayConfig、DeepSeekConfig)
```

- **Controller 层**：接收请求，参数校验，返回统一 `Result<T>` 响应。通过 `@CurrentUserId` 注解获取当前用户 ID，不从请求参数中取，防止伪造。
- **Service 层**：接口与实现分离，核心业务逻辑集中在此。高并发抢课、折扣计算、密码管理、AI 对话等都在这一层。
- **Mapper 层**：基于 MyBatis-Plus，联表查询返回 VO 而不返回 `Map<String, Object>`，保证类型安全。
- **横切层**：`GlobalExceptionHandler` 统一拦截异常转 `Result.fail`；`BusinessException` 携带 `ErrorCode` 枚举，前端可区分不同业务错误。

### 4.2 前端架构

```
Vue Router (beforeEach 五重守卫)
     ↓
Views (.vue) — 直接读 localStorage 管理用户状态
     ↓
src/api/*.js — 封装所有 HTTP 请求
     ↓
src/utils/request.js — Axios 实例，统一注入 token、拦截 401
```

前端路由守卫实现五重校验：白名单放行 → token/user 双重存在检查 → JWT exp 过期判断 → 管理员权限校验 → 后端 JwtInterceptor + Axios 401 拦截器兜底。

---

## 五、关键技术难点与解决方案

### 5.1 高并发抢课（核心难点）

**问题**：热门课程集中在特定时间开抢，瞬时并发量大，多用户同时操作可能导致库存超卖。比如库存只剩 1 个名额，两个用户同时读到 stock=1，都认为可以下单，最终卖出去 2 份。

**方案**：使用 **Redisson 分布式锁 + SQL 乐观条件双重兜底**。

```java
// 锁键: gym:booking:lock:{courseId} — 课程粒度，不同课程互不阻塞
RLock lock = redissonClient.getLock(lockKey);
try {
    lock.tryLock(3, 10, TimeUnit.SECONDS);  // 等3s抢锁，持锁上限10s
    // 临界区内完成全部"读-判断-写"操作：
    // ① 库存校验  ② 过期校验  ③ 重复预约校验  ④ 时间冲突校验
    // ⑤ SQL 原子扣减 (WHERE stock > 0 作为数据库层兜底)
    // ⑥ 折扣计算  ⑦ 写订单
} finally {
    if (lock.isHeldByCurrentThread()) lock.unlock(); // 防止误释放他人锁
}
```

设计要点：
- **锁粒度**：按 courseId 加锁，不同课程之间不互相阻塞，兼顾并发性能和数据安全。
- **双重兜底**：Java 层判断通过后，SQL 更新带 `WHERE stock > 0` 条件，防止极端并发下的漏判。
- **安全释放**：`isHeldByCurrentThread()` 判断后再 unlock，防止锁超时过期后误释放他人的锁。
- **订单号**：使用雪花算法（Hutool 的 `IdUtil.getSnowflakeNextIdStr()`）生成全局唯一订单号，不暴露自增规律。

### 5.2 订单状态机与并发安全

**问题**：取消订单、支付订单等操作如果在并发场景下不加控制，可能出现双重扣款、双倍退款、库存重复回退等问题。

**方案**：定义 `BookingStatus` 枚举统一管理状态，所有状态流转使用 **CAS（Compare And Swap）原子更新**。

```
PENDING(0) ──支付──→ PAID(1) ──取消──→ CANCELLED(2)
     │                                      ↑
     └──────────取消（回库存）───────────────┘
```

```java
// 支付时 CAS 更新：只有 PENDING → PAID 才允许，已支付的不会被重复扣款
int updated = mapper.update(null, new LambdaUpdateWrapper<CourseBooking>()
    .set(CourseBooking::getStatus, BookingStatus.PAID.getCode())
    .eq(CourseBooking::getId, bookingId)
    .eq(CourseBooking::getStatus, BookingStatus.PENDING.getCode()));
if (updated == 0) throw new BusinessException("订单状态异常，无法支付");
```

枚举内部封装了 `canPay()`、`canCancel()`、`isFinal()` 等业务判断方法，业务代码中不再出现 `status == 0` 这样的魔法数字，可读性和安全性都大幅提升。

### 5.3 安全机制

**鉴权链路**：

```
前端请求 Authorization: Bearer <JWT>
    ↓
JwtInterceptor.preHandle() — 解析 JWT，验签，提取 uid/role 写入 RequestAttribute
    ↓
Controller 通过 @CurrentUserId Long userId 获取当前用户
```

- **JWT**：使用 Hutool JWT 签发，payload 包含 uid、role、exp（2 小时过期），前端路由守卫解析 exp 本地判断过期，避免无效请求。
- **密码安全**：BCrypt 哈希存储，自带盐值，抗暴力破解。**向后兼容**：对历史明文密码，登录验证通过后自动升级为 BCrypt 写回，用户无感知。
- **防注入**：注册接口使用 DTO 接收参数，后端强制赋值 `role="user"`、`balance=0`、`vipType=0`，防止前端注入管理员权限或余额。
- **前后端双校验**：前端路由守卫拦截未授权访问，后端 JwtInterceptor + Axios 401 拦截器作为第二道防线。

### 5.4 设计模式应用 — 策略模式

**问题**：不同 VIP 等级享受不同折扣（普通原价、月卡 9 折、年卡 8 折），如果用 `if (vipType == 1) ... else if (vipType == 2)` 硬编码，新增 VIP 等级时需要修改所有折扣计算点。

**方案**：使用策略模式，定义 `DiscountStrategy` 接口 + `DiscountFactory` 工厂。

```java
public interface DiscountStrategy {
    BigDecimal calculate(BigDecimal originalPrice);
}

public class DiscountFactory {
    private static final Map<Integer, DiscountStrategy> strategies = new HashMap<>();
    static {
        strategies.put(0, price -> price);
        strategies.put(1, price -> price.multiply(new BigDecimal("0.9")));
        strategies.put(2, price -> price.multiply(new BigDecimal("0.8")));
    }
    public static BigDecimal calculatePrice(BigDecimal price, Integer vipType) {
        return strategies.getOrDefault(vipType, strategies.get(0)).calculate(price);
    }
}
```

调用方只需 `DiscountFactory.calculatePrice(price, vipType)`，完全不感知折扣细节。新增 VIP 等级只需在工厂中注册新策略，符合开闭原则。

### 5.5 AI 智能客服集成

系统集成了两套 AI 通道，共存互不干扰：

1. **Coze SDK 浮窗**（第三方）：通过 CDN 加载，所有页面（含登录页）出现浮窗按钮，提供基础问答。
2. **自建 AI 对话页面**（`AiChat.vue`）：左侧会话列表 + 右侧 **SSE 流式**聊天区，后端对接 DeepSeek API。

自建 AI 的技术亮点：
- **上下文注入**：每次请求拼接 System Prompt，包含用户实时余额、VIP 折扣、可预约课程列表、最近预约记录，AI 能给出个性化推荐。
- **多轮对话**：携带最近 20 条历史消息，控制 token 消耗。
- **SSE 流式推送**：新线程中逐 chunk 推送，客户端断连时保存已接收内容，不丢失对话记录。
- **开关控制**：`deepseek.enabled` 配置项，关闭时全局禁用 AI 功能。

### 5.6 支付宝支付集成

系统集成支付宝沙箱支付，支持课程支付和余额充值两种模式。支付流程为：用户发起支付 → 后端生成支付表单 → 前端跳转支付宝 → 支付完成 → 同步回调跳转落地页 → 后端查询订单状态确认。异步通知路径也已配置，生产环境切换真实商户号即可。

---

## 六、数据库设计

核心表结构：

| 表名 | 说明 | 关键字段 |
|---|---|---|
| `sys_user` | 用户表 | username, password(BCrypt), role, balance, vipType, vipExpireTime, status |
| `gym_course` | 课程表 | name, coach, category, startTime, capacity, stock, price, version(乐观锁) |
| `gym_booking` | 预约订单表 | userId, courseId, bookingNo(雪花ID), status, realPrice, createTime |
| `ai_chat_session` | AI 会话表 | userId, title, createTime, updateTime |
| `ai_chat_message` | AI 消息表 | sessionId, role(user/assistant), content, createTime |

---

## 七、项目亮点总结

1. **高并发抢课的完整方案**：Redisson 分布式锁（courseId 粒度）+ SQL 乐观条件双重兜底 + CAS 状态流转，从设计上杜绝超卖。
2. **安全纵深防御**：BCrypt 密码哈希 + 明文无感升级 + JWT 鉴权 + 前后端双重校验 + DTO 防注入，形成多层安全防护。
3. **设计模式落地**：策略模式处理折扣，枚举 + 状态机处理订单状态，做到了"开闭原则"和"高内聚低耦合"。
4. **AI 赋能**：自建 SSE 流式 AI 客服，上下文感知用户实时信息，提供个性化推荐。
5. **数据可视化**：ECharts 6 实现数据驾驶舱，提供运营决策支持。
6. **架构决策记录（ADR）**：项目中 5 份 ADR 文档记录了分布式锁选型、策略模式、密码方案、枚举设计、VO 规范等关键决策及其理由，使项目的"为什么这样做"有据可查。

---

## 八、不足与改进方向

1. **测试覆盖不足**：`src/test` 目前为空骨架，缺乏单元测试和集成测试。后续计划引入 JUnit 5 + Testcontainers 补全，尤其要覆盖高并发抢课场景。
2. **状态管理**：前端目前直接读写 localStorage，无 Pinia/Vuex 统一状态管理，后续可引入 Pinia 提升可维护性。
3. **配置外化**：部分敏感配置（如 JWT 密钥、支付宝密钥）仍硬编码在 yml 中，后续应通过环境变量注入。
4. **前后端状态同步**：VIP 过期检查仅在登录时触发，下单时不会重新校验，后续可改为实时校验。

---

以上是我的项目介绍，请各位老师批评指正，谢谢！