你为什么选用springboot和vue，优势是什么?

## 回答

### Spring Boot 的优势

1. **快速开发**：Spring Boot 的"约定优于配置"理念，自动配置和起步依赖让项目搭建从原来的几天缩短到几小时。比如集成了 MyBatis-Plus Starter 后，只需配 yml 和写 Mapper 接口，不用再手动配置 SqlSessionFactory、DataSource 等。
2. **生态成熟**：Spring 生态几乎覆盖了企业开发的全部需求——Spring Security（密码加密）、Spring MVC（Web 层）、Spring Transaction（事务管理），而且社区庞大，遇到问题容易找到解决方案。
3. **微服务友好**：内置 Tomcat，打成 jar 包直接运行，配合 Docker 部署非常方便。项目虽然目前是单体架构，但分层设计清晰，未来拆微服务成本低。
4. **生产级特性**：健康检查、指标监控（Actuator）、外部化配置等开箱即用。

### Vue 3 的优势

1. **Composition API**：相比 Vue 2 的 Options API，逻辑复用更方便，复杂组件的代码组织更清晰。本项目中的 `AiChat.vue` 就是典型例子，SSE 流式处理逻辑用 Composition API 封装得很干净。
2. **Vite 构建**：开发服务器冷启动几乎是秒级，HMR 热更新极快，开发体验远超 Webpack。
3. **学习曲线平缓**：Vue 的模板语法接近原生 HTML，单文件组件（.vue）将模板、逻辑、样式放在一起，对于小团队开发效率很高。
4. **生态配套**：Element Plus 提供了企业级 UI 组件库，ECharts 6 覆盖了数据可视化需求，Vue Router 4 处理路由和权限守卫，整个技术栈配合默契。

### 为什么前后端分离？

1. **并行开发**：前后端团队可以独立开发，只要约定好 API 接口规范即可。
2. **独立部署**：前端是纯静态资源，可以部署到 Nginx/CDN；后端单独部署，互不影响。
3. **技术选型灵活**：前后端可以各自选择最适合的技术栈，比如前端用 Vue + Element Plus 做富交互 UI，后端用 Java 做高并发抢课的核心逻辑。
4. **复用性**：同一套后端 API 可以同时服务 Web 端、小程序端、App 端。

