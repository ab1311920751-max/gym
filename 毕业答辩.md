# 健身房管理系统 · 毕业答辩手册

> 项目：基于 Spring Boot + Vue 3 的健身房综合管理系统  
> 技术栈：Spring Boot 3 / MyBatis-Plus / MySQL / Redis / Vue 3 / Element Plus / ECharts / DeepSeek AI

---

## 一、答辩整体流程

### 1.1 时间安排（通常 15–20 分钟/人）

| 阶段 | 时长 | 内容 |
|------|------|------|
| 自我介绍 | 1 分钟 | 姓名、专业、导师 |
| 项目陈述 | 5–8 分钟 | PPT 演示，讲解系统背景、功能、技术架构 |
| 现场演示 | 2–3 分钟（可选） | 运行程序展示核心功能 |
| 老师提问 | 5–8 分钟 | 2–4 位老师轮流提问，每人 1–3 个问题 |
| 总结退场 | 1 分钟 | 感谢致辞，等候成绩 |

---

### 1.2 开场陈述模板（背熟，控制在 6 分钟内）

> 各位老师好，我叫 \_\_\_\_，来自 \_\_\_\_ 专业，我的毕业设计题目是《基于 Spring Boot 与 Vue 3 的健身房综合管理系统》，指导老师是 \_\_\_\_ 老师。下面我来汇报本系统的设计与实现。
>
> **一、选题背景**
> 随着健身行业快速发展，传统健身房依赖纸质记录或简单 Excel 管理，存在课程预约冲突、账务混乱、会员体验差等痛点。本系统旨在提供一个完整的数字化管理平台，覆盖用户端预约、管理端运营和数据可视化三个维度。
>
> **二、系统功能**
> 系统分为用户和管理员两个角色。用户可以注册登录、浏览并预约课程、通过支付宝完成支付、管理个人钱包和 VIP 会员，以及与 AI 助手对话获取健身建议。管理员可以管理课程、用户、Banner 轮播，并通过数据驾驶舱查看经营数据。
>
> **三、技术架构**
> 后端采用 Spring Boot 3 + MyBatis-Plus + MySQL，使用 Redisson 分布式锁解决高并发抢课问题，JWT 实现无状态鉴权，BCrypt 进行密码加密，折扣计算采用策略模式。前端采用 Vue 3 + Vite + Element Plus，ECharts 6 实现数据可视化，并集成 DeepSeek AI 实现流式对话。
>
> **四、核心亮点**
> 第一，高并发预约场景下的分布式锁方案；第二，AI 客服双通道（Coze 浮窗 + 自建 DeepSeek SSE）；第三，折扣策略模式，新增 VIP 等级无需修改业务代码；第四，支付宝沙箱完整对接，包含异步回调和同步跳转两条链路。
>
> 以上是我的汇报，请各位老师批评指正。

---

## 二、答辩问题与应对

> 难度标识：⭐ 简单 &nbsp;&nbsp; ⭐⭐ 中等 &nbsp;&nbsp; ⭐⭐⭐ 较难 &nbsp;&nbsp; ⭐⭐⭐⭐ 刁钻

---

### 第一组：项目背景与需求

---

**Q1 ⭐ 你为什么选择做健身房管理系统？市面上已经有现成产品了吧？**

> **答：**
> 选题出发点主要有两个：一是身边健身房的真实痛点——我去的健身房还在用微信群抢课，经常出现超员和冲突；二是技术学习价值——健身房场景天然包含并发预约、支付、会员体系等典型企业级问题，能综合练习后端分布式、前端交互、第三方接入等核心技能。
>
> 市面上产品确实存在，但大多是 SaaS 收费产品，中小健身房负担重。本系统作为开源学习项目，重点在于工程能力的综合训练，而非商业竞争。

---

**Q2 ⭐ 你的系统有哪些用户角色？权限是怎么区分的？**

> **答：**
> 系统有两种角色：`admin`（管理员）和 `user`（普通用户），存储在 `sys_user` 表的 `role` 字段中。
>
> 权限控制分两层：
> - **前端**：Vue Router 的 `beforeEach` 守卫检查，所有以 `/admin-` 开头的路由都会验证 `role === 'admin'`，否则跳首页并提示无权访问；
> - **后端**：`JwtInterceptor` 从请求头解析 JWT，将 `uid` 和 `role` 写入 `RequestAttribute`，敏感接口（如禁用用户）在 Service 层二次校验角色。
>
> 这样设计是"前端拦 UX，后端拦安全"——前端防止误操作，后端才是真正的权限防线。

---

### 第二组：数据库设计

---

**Q3 ⭐ 你的数据库有几张表？设计时有没有遵循范式？**

> **答：**
> 数据库共 8 张主要表：`sys_user`（用户）、`gym_course`（课程）、`course_booking`（预约订单）、`banner`（轮播图）、`ai_chat_session`（AI 会话）、`ai_chat_message`（AI 消息），另有支付宝回调相关辅助表。
>
> 整体遵循第三范式：消除冗余依赖，例如课程信息单独一张表，预约表通过 `course_id` 外键关联而不是冗余课程名。但有一处刻意反范式：`course_booking` 里保留了 `real_price`（实际支付金额），因为 VIP 折扣可能变动，下单时的价格需要永久固化，这是业务上的合理冗余。

---

**Q4 ⭐⭐ course_booking 表里的 bookingNo 是干什么用的？直接用主键 id 不行吗？**

> **答：**
> `bookingNo` 是对外暴露的业务单号（如 `GYM20240604123456001`），`id` 是数据库自增主键，两者用途不同：
>
> 1. **安全性**：如果用自增 `id` 对外，外部可以遍历推测订单总量，`bookingNo` 通过时间戳+随机数组合，不暴露内部信息；
> 2. **对接支付宝**：支付宝回调携带的 `out_trade_no` 必须是可读的业务单号，用 `bookingNo` 做映射更清晰；
> 3. **解耦**：将来数据库迁移或分库，主键可能变化，对外的业务单号保持稳定。

---

**Q5 ⭐⭐ VIP 到期时间你是怎么存的？如果用户续费 VIP 怎么处理？**

> **答：**
> `sys_user` 表有 `vip_expire_time` 字段，类型是 `DATETIME`。
>
> 续费逻辑在 `UserServiceImpl.buyVip()` 中处理：先取出当前的 `vipExpireTime`，如果还未过期，就在当前到期时间基础上**叠加**购买时长；如果已过期或者是首次购买，则从**当前时间**开始计算。这样保证续费不会损失剩余时长，类似视频平台的会员续费逻辑。

---

### 第三组：后端技术细节

---

**Q6 ⭐ JWT 是什么？你为什么用 JWT 而不是 Session？**

> **答：**
> JWT（JSON Web Token）是一种无状态的认证令牌，由 Header.Payload.Signature 三部分 Base64 编码拼接而成，服务端不存储任何 session 信息，通过签名验证真伪。
>
> 选 JWT 的原因：
> - **无状态**：服务端不需要维护 session 存储，天然支持水平扩展，多实例部署不用考虑 session 共享；
> - **前后端分离适配**：移动端/Web 都直接携带 token，比 Cookie 跨域更灵活；
> - 本项目单机部署，Session 也能用，但 JWT 是更主流的前后端分离方案，工程实践价值更高。
>
> 缺点是 JWT 一旦签发无法主动失效，只能等 `exp` 过期。本项目通过前端路由守卫检测 `exp` 字段来处理过期跳转。

---

**Q7 ⭐⭐ 你用 BCrypt 加密密码，BCrypt 有什么特点？MD5 不行吗？**

> **答：**
> MD5 不行，主要有两个问题：一是 MD5 是确定性哈希，相同密码始终产生相同摘要，攻击者可以用彩虹表反查；二是 MD5 计算非常快，暴力枚举成本低。
>
> BCrypt 的核心优势：
> 1. **加盐**：每次哈希自动生成随机 salt，同一密码每次结果不同，彩虹表完全失效；
> 2. **故意慢**：内置 cost 因子（本项目默认 10），每次验证需要数十毫秒，暴力破解成本极高；
> 3. **自包含**：salt 嵌入哈希结果中，验证时无需单独存 salt。
>
> 此外，项目里还有旧明文密码的向后兼容逻辑：老数据登录成功后自动将明文升级为 BCrypt 存回数据库，不需要强制用户改密码。

---

**Q8 ⭐⭐⭐ 你怎么解决抢课时的超卖问题？说说你的分布式锁方案。**

> **答：**
> 超卖的根本原因是"读-判断-写"三步不是原子操作，并发时多个请求都读到剩余库存 > 0，然后都去扣减，导致库存变负。
>
> 我的方案是 **Redisson 分布式锁**，锁的粒度是课程 ID，键格式为 `gym:booking:lock:{courseId}`。
>
> 核心代码逻辑：
> ```
> RLock lock = redisson.getLock("gym:booking:lock:" + courseId);
> boolean locked = lock.tryLock(3, 10, TimeUnit.SECONDS);  // 等待3秒，持有最多10秒
> if (!locked) throw new BusinessException(BIZ_LOCK_TIMEOUT);
> try {
>     // 临界区：校验库存 → 检查重复/冲突 → 扣减库存 → 写订单
> } finally {
>     lock.unlock();  // 无论异常都释放
> }
> ```
>
> 三点设计原则：所有"读+判断+写"在锁内原子完成；锁粒度是课程级而非全局锁，不同课程互不影响，吞吐量高；`finally` 保证异常时锁一定释放，不会死锁。
>
> 为什么用 Redisson 而不是 `synchronized`？因为 `synchronized` 是 JVM 级别的，集群部署时每台机器的锁互相独立，无法跨进程互斥。Redis 是集中式存储，天然跨进程。

---

**Q9 ⭐⭐ 你说到策略模式做折扣计算，具体怎么设计的？**

> **答：**
> 折扣涉及三种 VIP 类型（普通 0 折扣、月卡 9 折、年卡 8 折），最直接的写法是 `if-else` 或 `switch`，但每新增一种 VIP 就要改业务代码，违反开闭原则。
>
> 策略模式的做法：
> 1. 定义 `DiscountStrategy` 接口，只有一个方法 `apply(BigDecimal price)`；
> 2. 每种 VIP 各实现一个策略类，如 `MonthlyVipStrategy`（乘以 0.9）、`AnnualVipStrategy`（乘以 0.8）；
> 3. `DiscountFactory` 维护一个 `Map<Integer, DiscountStrategy>`，按 `vipType` 取对应策略；
> 4. 调用方只写 `DiscountFactory.get(user.getVipType()).apply(course.getPrice())`，完全不感知 VIP 类型。
>
> 新增 VIP 等级只需加一个策略类并在工厂注册，原有代码零修改。

---

**Q10 ⭐⭐⭐ MyBatis-Plus 和 MyBatis 有什么区别？你为什么用 MyBatis-Plus？**

> **答：**
> MyBatis-Plus 是 MyBatis 的增强框架，在 MyBatis 基础上提供了：
> - **通用 CRUD**：继承 `BaseMapper<T>` 后，`insert`、`selectById`、`updateById`、`deleteById` 等方法开箱即用，不用写 XML；
> - **条件构造器**：`LambdaQueryWrapper` 用 Lambda 表达式引用字段，避免字符串硬编码字段名，重构时不会漏改；
> - **分页插件**：`Page<T>` + `PaginationInnerInterceptor` 自动处理 `LIMIT` 语句；
> - **自动填充**：`@TableField(fill = FieldFill.INSERT)` 可以自动填充 `createTime` 等公共字段。
>
> 本项目用 MyBatis-Plus 大幅减少了 Mapper XML 的编写量，复杂查询（如多条件课程筛选）用 `LambdaQueryWrapper` 动态拼接即可，简洁且类型安全。

---

**Q11 ⭐⭐⭐ Spring Boot 3 和 Spring Boot 2 有什么主要区别？**

> **答：**
> Spring Boot 3 的核心变化：
> 1. **最低 JDK 17**：利用了 Record、sealed class 等新语法特性；
> 2. **Jakarta EE 10**：所有 `javax.*` 包名改为 `jakarta.*`，如 `javax.servlet` → `jakarta.servlet`，迁移时需要批量替换；
> 3. **原生镜像支持**：通过 GraalVM 可以编译为原生可执行文件，启动时间和内存占用大幅降低；
> 4. **观测性增强**：Micrometer 深度集成，与 Prometheus/Grafana 对接更方便。
>
> 本项目主要感受到的差异是包名变化。我最初引入 `HandlerInterceptor` 时编译报错，排查发现是 Jakarta 包名问题，修改 import 后解决。

---

### 第四组：前端技术细节

---

**Q12 ⭐ Vue 3 和 Vue 2 有什么主要区别？**

> **答：**
> 主要有三点：
> 1. **Composition API**：Vue 3 引入 `setup()`，用 `ref`、`reactive`、`computed`、`watch` 组合逻辑，相比 Vue 2 的 Options API，相关逻辑可以聚合在一起而不是分散在 `data`、`methods`、`computed` 各处，复用更方便；
> 2. **性能提升**：响应式系统从 `Object.defineProperty` 改为 `Proxy`，支持数组下标和动态属性的监听，不再需要 `Vue.set()`；虚拟 DOM 做了静态节点提升等编译优化；
> 3. **更好的 TypeScript 支持**：整个框架用 TS 重写，类型推导更准确。
>
> 本项目使用的是 Options API 风格（`data`、`methods`、`mounted`），保持了与 Vue 2 类似的写法，上手门槛低，后续可以按需迁移到 Composition API。

---

**Q13 ⭐⭐ 你的前端没有用 Pinia 或 Vuex，状态管理是怎么做的？有没有什么问题？**

> **答：**
> 当前用 `localStorage` 直接存取用户信息，各组件通过 `JSON.parse(localStorage.getItem('user'))` 获取。
>
> 这样做的优点是简单，不引入额外依赖，对于这个体量的项目够用。
>
> 存在的问题：
> 1. **响应式缺失**：`localStorage` 变更不会触发 Vue 的重新渲染，若多个组件同时展示用户信息（如余额），一个组件更新了 `localStorage`，其他组件不会自动刷新；
> 2. **类型不安全**：每次都要手动 `JSON.parse` 和做空值判断，代码冗余且容易出错；
> 3. **XSS 风险**：`localStorage` 可被 JS 脚本读取，存在 XSS 攻击向量。
>
> 如果项目规模变大，建议引入 Pinia，将用户信息放在 store 中，通过响应式机制自动同步所有组件。

---

**Q14 ⭐⭐ ECharts 的图表是怎么接入的？数据从哪里来？**

> **答：**
> 接入流程：在组件 `mounted` 钩子中调用 `echarts.init(dom)` 初始化实例，然后调用后端 `/report/dashboard` 接口获取统计数据，将数据格式化后通过 `chart.setOption(option)` 渲染。
>
> 后端 `ReportController` 聚合了四类数据：注册用户总数、订单总数、总收入、VIP 分布（普通/月卡/年卡各多少人）。前端将 VIP 分布数据渲染为饼图，将近 7 天新增用户/订单渲染为折线图。
>
> 注意事项：组件销毁时要调用 `chart.dispose()` 防止内存泄漏；窗口 resize 时要调用 `chart.resize()` 保持响应式。

---

**Q15 ⭐⭐⭐ 你的 AI 对话是怎么实现流式输出的？**

> **答：**
> 流式输出用的是 **SSE（Server-Sent Events）**，而不是 WebSocket。
>
> 后端 `AiController` 的 `/ai/chat/stream` 接口返回 `SseEmitter`，`AiServiceImpl` 调用 DeepSeek API 的流式接口（`stream: true`），每收到一个 chunk 就通过 `emitter.send(chunk)` 推给前端。
>
> 前端用浏览器原生的 `EventSource` API 或 `fetch` + `ReadableStream` 消费流，每收到一段文字就拼接到消息气泡末尾，实现打字机效果。
>
> 选 SSE 而不是 WebSocket 的原因：AI 对话是单向推送（服务端→客户端），SSE 天然适合，且基于 HTTP，不需要额外的握手协议，穿越代理和防火墙的兼容性更好，实现也更简单。

---

### 第五组：系统设计与架构

---

**Q16 ⭐⭐ 你的系统如何保证接口安全？普通用户能调管理员接口吗？**

> **答：**
> 分三层防护：
>
> 第一层，前端路由守卫：访问 `/admin-*` 路由时校验 `role`，普通用户直接被拦截在 UI 层，不会发出请求。
>
> 第二层，JWT 拦截器：所有请求到达 Controller 前，`JwtInterceptor` 解析 token，提取 `uid` 和 `role` 写入 `RequestAttribute`。token 缺失或非法直接返回 401。
>
> 第三层，Service 层校验：敏感操作（如禁用用户）在 Service 内部再次校验 `role`，即使前两层被绕过，操作仍会失败并抛 `UnauthorizedException`。
>
> 此外，获取当前用户 ID 强制通过 `@CurrentUserId` 注解从 token 解析，不接受请求体里的 userId 参数，防止越权操作他人数据。

---

**Q17 ⭐⭐⭐ 支付宝回调为什么分同步回调和异步回调两条链路？**

> **答：**
> 这是支付宝支付的标准设计：
>
> - **同步回调（return_url）**：用户支付完成后，支付宝浏览器跳转到 `return_url`（本项目是 `/pay/success` 页面）。这条链路**不可靠**——用户可能关浏览器、网络断开等，不能依赖它来确认支付成功，只用于 UI 层的跳转体验。
>
> - **异步回调（notify_url）**：支付宝服务器主动 POST 到 `notify_url`，有重试机制（最多重试 25 次，持续 24 小时），可靠性高，是**真正确认订单状态的依据**。
>
> 本项目是本地开发环境，`notify_url` 指向 `localhost`，支付宝服务器无法访问，所以异步回调收不到。实际处理方式是依赖 `return_url` 跳转 + 前端主动查询订单状态来完成确认。生产环境需要公网地址并做签名验证，防止伪造回调。

---

**Q18 ⭐⭐ 你的系统有哪些地方可以优化？（考察自我认知）**

> **答：**
> 主要有以下几个方向：
>
> 1. **缓存层缺失**：课程列表每次都查数据库，可以加 Redis 缓存，命中时直接返回，课程变更时失效缓存；
> 2. **状态管理**：前端无 Pinia，局部状态更新不同步，引入 Pinia 后可以全局响应式共享用户信息；
> 3. **测试覆盖**：目前测试骨架为空，关键路径（预约、支付、折扣）应有单元测试和集成测试；
> 4. **接口文档**：缺少 Swagger/OpenAPI 文档，接口联调依赖手写文档；
> 5. **图片上传**：Banner 和课程封面目前存 URL 字符串，没有文件上传功能，实际场景需要对接 OSS；
> 6. **限流**：高并发接口（抢课）目前只有分布式锁，可以加令牌桶/漏桶限流，防止系统被压垮。

---

**Q19 ⭐⭐⭐ 如果系统要支持 10 万用户同时抢课，你的方案还够用吗？**

> **答：**
> 当前方案在小规模下有效，但面对 10 万并发是不够的，原因和改进方向如下：
>
> **瓶颈分析：**
> - Redisson 分布式锁是串行执行，10 万请求会排队，锁等待时间指数上涨，大部分请求会超时拿不到锁；
> - 单机 MySQL 写入是瓶颈；
> - 单机 Spring Boot 实例 QPS 有上限。
>
> **改进思路：**
> 1. **预扣库存到 Redis**：将库存移到 Redis（`DECR` 操作是原子的），请求先对 Redis 减库存，减成功则进消息队列，失败直接返回售罄。绝大多数请求在 Redis 层就被过滤；
> 2. **消息队列削峰**：用 RocketMQ/Kafka 异步写订单，数据库压力变成平稳的消费速率；
> 3. **水平扩展**：Spring Boot 多实例 + Nginx 负载均衡，Redis 和 MQ 是共享中间件，无状态服务天然可水平扩展；
> 4. **CDN + 静态资源分离**：前端静态资源走 CDN，减轻服务器压力。

---

### 第六组：刁钻问题（⭐⭐⭐⭐）

---

**Q20 ⭐⭐⭐⭐ Redisson 的 tryLock 第一个参数（等待时间）和第二个参数（持有时间）分别设多少合适？你是怎么考虑的？**

> **答：**
> 我设的是 `tryLock(3, 10, SECONDS)`，即等待 3 秒、最长持有 10 秒，这两个值都是结合业务场景权衡的：
>
> **等待时间（3 秒）：**
> 用户点击"抢课"按钮后，能接受短暂等待但不能超过 5 秒以上（UX 底线）。如果热门课程争抢激烈，临界区处理很快（几十毫秒），3 秒内排队的请求基本都能拿到锁。设太短（如 0.1 秒）会让大量用户看到"人数过多"而实际库存还有，体验差；设太长（如 30 秒）会挂起大量线程，资源耗尽。
>
> **持有时间（10 秒）：**
> 持有时间是防止业务崩溃后锁永久不释放的保险。临界区只有数据库读写，正常情况 < 100ms，设 10 秒是足够的安全边界。设太短（如 1 秒）有风险：如果 GC 停顿或数据库慢查询超过 1 秒，锁自动过期但代码还在跑，另一个线程拿到锁并发执行，超卖逻辑重现。
>
> 这两个参数没有通用最优解，需要根据 P99 响应时间和业务容忍度压测调整。

---

**Q21 ⭐⭐⭐⭐ JWT token 被截获了怎么办？你们系统有没有防重放攻击？**

> **答：**
> 这是当前系统的一个安全弱点，我如实说明现状和改进方案：
>
> **现状：**
> 当前 JWT 没有做防重放。若 token 在有效期内被截获，攻击者可以完全冒充用户操作，直到 token 过期。
>
> **缓解措施（已有）：**
> - HTTPS 传输加密，截获难度高；
> - token 有效期设置合理（比如 2 小时）；
> - 前端存 `localStorage` 而非 cookie，减少 CSRF 风险（但增加 XSS 风险）。
>
> **可以改进的方向：**
> 1. **短 token + 刷新机制**：access_token 有效期缩短到 15 分钟，配合 refresh_token（7 天）静默续期，即使截获，窗口期很短；
> 2. **黑名单**：用户主动登出时将 token 的 `jti`（唯一标识）加入 Redis 黑名单，后续请求到拦截器时查黑名单，命中则拒绝；
> 3. **设备绑定**：token 签发时绑定 IP 或 User-Agent，其他设备使用则失效（用户体验有所损失）。
>
> 防重放严格意义上还需要每次请求携带随机 nonce，服务端记录已用 nonce，但对于本项目体量过度设计了。

---

**Q22 ⭐⭐⭐⭐ 你用乐观锁（version 字段）还是悲观锁（Redisson）来防超卖？能不能两种都不用，只靠数据库事务？**

> **答：**
> 项目里选的是 Redisson 悲观锁。`GymCourse` 实体有 `version` 字段，但 `@Version` 注解目前注释掉了，没有启用。让我分析三种方案的差异：
>
> **只靠数据库事务（不行）：**
> 事务的 ACID 保证数据一致性，但默认隔离级别（RR）下，两个并发事务都能读到同一个库存值并各自提交扣减，这是事务隔离没有解决的写-写冲突。必须用 `SELECT ... FOR UPDATE`（悲观锁）或数据库层面的乐观锁才能解决。
>
> **数据库乐观锁（version）：**
> `UPDATE gym_course SET stock = stock - 1, version = version + 1 WHERE id = ? AND version = ?`。并发时只有一个能匹配到 version 成功，其他都更新 0 行，应用层检测到后重试。优点是无需分布式组件；缺点是高并发时重试风暴，大量请求反复重试给数据库施压。
>
> **Redisson 悲观锁（当前方案）：**
> 请求串行进入临界区，无重试。代价是引入 Redis 中间件依赖，Redis 宕机抢课功能完全不可用。
>
> 本项目选悲观锁，因为课程库存操作复杂（要做时间冲突、重复预约等多重校验），乐观锁重试时这些校验也要重跑，逻辑复杂且效率低；悲观锁让临界区逻辑更清晰可控。

---

**Q23 ⭐⭐⭐⭐ 你的 DeepSeek API key 写在配置文件里，这安全吗？**

> **答：**
> 不安全，这是开发阶段的便利写法，生产环境绝对不能这样做。原因：配置文件可能随代码提交到 Git 仓库，一旦仓库泄露（或公开），API key 就暴露了。
>
> 项目里已经做了一定改进：`application.yml` 中写的是 `${DEEPSEEK_API_KEY:sk-xxx}`，意思是**优先读环境变量 `DEEPSEEK_API_KEY`**，环境变量不存在才回退到冒号后面的默认值。生产部署时通过 Docker 的 `-e DEEPSEEK_API_KEY=xxx` 或 K8s Secret 注入，配置文件里永远不出现真实 key。
>
> 更进一步的做法是接入 Vault 或云厂商的 KMS，做密钥的集中管理和定期轮换。
>
> 目前 `.gitignore` 没有单独忽略 `application.yml`，如果有真实 key 提交到仓库，应立即在 DeepSeek 后台吊销并重新生成。

---

## 三、答辩加分项与注意事项

### 3.1 加分项

- **主动承认不足**：老师问到系统缺陷时，坦诚说"这是当前版本的局限，改进方向是……"，比强行辩护得分更高；
- **给出数字**：回答性能问题时能说出"临界区执行约 50–100ms""tryLock 等待 3 秒"比模糊说"很快"更专业；
- **说清楚为什么**：每个技术选型都有"为什么不用 A 而用 B"的对比，说出来展示你了解技术全貌；
- **联系代码**：适时说"这部分对应 `BookingServiceImpl` 第 78 行"，展示你真的写了这段代码。

### 3.2 注意事项

- 提问时间控制：**每个回答不超过 2 分钟**，说清楚核心逻辑即可，不要展开讲所有细节；
- 不知道的问题：直接说"这个问题我研究不深入，我的理解是……，不确定是否准确，欢迎老师指正"；
- 不要争辩：老师给出"你这里有问题"时，先说"谢谢老师指出，这里确实存在……"，再解释；
- 演示前先检查：确认后端、数据库、Redis 全部启动，演示账号能正常登录，避免现场翻车。

### 3.3 最可能被追问的三个点

1. **分布式锁超卖问题**（必问，要能手写核心逻辑）
2. **JWT 鉴权流程**（必问，要能画出前端→拦截器→Controller 的完整链路）
3. **为什么选这些技术**（每个技术选型要有"对比 + 理由"，不能说"因为流行"）
